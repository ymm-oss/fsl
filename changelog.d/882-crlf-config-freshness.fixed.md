Fixed (#882): text checkouts are pinned to LF and source-inspecting controls normalise CRLF reads, so freshness and adjacent-line checks do not depend on checkout configuration. Found by the promotion-only Windows solver lane after #880 merged, because that lane is skipped on pull requests.
