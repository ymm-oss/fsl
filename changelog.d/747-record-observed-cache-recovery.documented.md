Documented (#747): Recorded the observed Windows native-Z3 timeout recovery after #794: the
`cache-on-failure` post step saved on the cancelled run, the following warm run completed, cache
usage measured 7.469 GiB, and the restore-only FSL Logic Test logged a full cache match before
completing in 3m02s.
