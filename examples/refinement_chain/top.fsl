// Top of the chain (business contract). A request moves from Open to Done.
spec ChainTop {
  type Id = 0..1
  enum TSt { TOpen, TDone }
  state { st: Map<Id, TSt> }
  init { forall c: Id { st[c] = TOpen } }
  fair action finish(c: Id) { requires st[c] == TOpen   st[c] = TDone }
}
