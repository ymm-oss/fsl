// SPDX-License-Identifier: Apache-2.0

// The requirements-document approval path must still reach the shared FSL
// parser before it reads or validates its rendered-document artifact.
spec EnvelopeParityBrokenApprovalRequirementsDocument {
  state { x: Int }
  init { x = }
}
