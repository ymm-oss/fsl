// SPDX-License-Identifier: Apache-2.0

spec BoundedCounter {
  state { x: Int, y: Int }
  init { x = 0  y = 0 }

  action step() {
    requires x >= 0 and x < 4
    x = x + 1
    y = y + 1
  }

  invariant Sync { y <= 4 }
}
